Floating point control register initial value. The *worker*
floating-point control register `$FP_CTL` is initialised with the
contents of this register prior to worker activation.

See `run` and `runall`
